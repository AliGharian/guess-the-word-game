export enum GameState {
  START = "start",
  PLAYING = "playing",
  WON = "won",
  LOST = "lost",
}

export enum GameTurn {
  USER = "user",
  ROBOT = "robot",
}

export enum GameMode {
  EASTY = "easy",
  REGULAR = "regular",
  HARD = "hard",
}
